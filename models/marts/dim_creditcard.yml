version: 2

models:
  - name: dim_creditcard
    columns:
      - name: creditcard_sk
        description: The primary key of the credit card dimension.
        tests:
          - unique
          - not_null